<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.openfinance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenFinance">
        <activity android:name=".support"></activity>
        <activity android:name=".insights" />
        <activity android:name=".offer2" />
        <activity android:name=".offermap" />
        <activity android:name=".scratch" />
        <activity android:name=".rewards2" />
        <activity android:name=".rewards1" />
        <activity android:name=".homepage" />
        <activity android:name=".loginpage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>